#include <stdio.h>
#include <stdlib.h>
void selection_sort(int a[], int n);
/*int main()
{
    int n;
    printf("Enter the size of array:\n");
    scanf("%d",&n);
    int a[n];
    printf("Enter the elements:\n");
    for(int i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
    selection_sort(a,n);
    return 0;
}

void selection_sort(int a[], int n)
{
    printf("Array is :\n");
    for(int i=0; i<n; i++)
    {
        printf("%d \t",a[i]);
    }
    for(int i=0; i<n-1; i++)
    {
        int temp;
        int min=i;
        for(int j=i+1; j<n; j++)
        {
            if(a[min]>a[j]){
            min =j;
            }
        }
        temp = a[i];
        a[i] = a[min];
        a[min] = temp;
    }
    printf("\nSorted Array :\n");
    for(int i=0; i<n; i++)
    {
        printf("%d \t",a[i]);
    }

}*/

int main()
{
    int n;
    printf("Enter the size of array:\n");
    scanf("%d",&n);
    int a[n];
    printf("Enter the elements:\n");
    for(int i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
    printf("Array is :\n");
    for(int i=0; i<n; i++)
    {
        printf("%d \t",a[i]);
    }
    for(int i=0; i<n-1; i++)
    {
        int temp;
        int min=i;
        for(int j=i+1; j<n; j++)
        {
            if(a[min]>a[j]){
            min =j;
            }
        }
        temp = a[i];
        a[i] = a[min];
        a[min] = temp;
    }
    printf("\nSorted Array :\n");
    for(int i=0; i<n; i++)
    {
        printf("%d \t",a[i]);
    }
    return 0;
}
